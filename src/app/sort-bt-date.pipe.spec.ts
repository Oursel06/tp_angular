import { SortBtDatePipe } from './sort-bt-date.pipe';

describe('SortBtDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortBtDatePipe();
    expect(pipe).toBeTruthy();
  });
});
